using UnityEngine;

/// <summary>
/// 이펙트를 소환하는 이펙트 매니저
/// 
/// </summary>
public class EffectManager : ISingleton<EffectManager>
{
    public GameObject Slash;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
